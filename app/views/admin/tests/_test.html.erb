<tr>
  <td><%= test.id %></td>
  <td><%= test.title %></td>
  <td><%= test.level %></td>
  <td><%= link_to test.questions.count, admin_test_questions_path(test.id) %></td>
  <td><%= link_to t('common.edit'), edit_admin_test_path(test) %></td>
  <td><%= link_to t('common.show'), admin_test_path(test) %></td>
  <td><%= link_to t('common.delete'), admin_test_path(test), method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>
