<table border="1">
   <caption><%= @test.title %></caption>
  <tr>
    <th><%= t('.id') %></th>
    <th><%= t('.question') %></th>
    <th colspan="3"><%= t('.action') %></th>
  </tr>
  <%= render @questions %>
</table>

<%= link_to t('.new_question'), new_admin_test_question_path %>
<%= link_to t('common.back'), admin_tests_path %>
