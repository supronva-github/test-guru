<tr>
  <td><%= answer.id %></td>
  <td><%= answer.body %></td>
  <td><%= answer.correct %></td>
  <td><%= link_to t('common.edit'), edit_admin_answer_path(answer) %></td>
  <td><%= link_to t('common.show'), admin_answer_path(answer) %></td>
  <td><%= link_to t('common.delete'), admin_answer_path(answer), method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>
